version: '3.8'

services:
  db:
    image: postgres:16-alpine
    container_name: tpv_2026_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: tpv_2026
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  pgadmin:
    image: dpage/pgadmin4
    container_name: tpv_2026_pgadmin
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@tpv.com
      PGADMIN_DEFAULT_PASSWORD: admin
    ports:
      - "8080:80"
    depends_on:
      - db

volumes:
  postgres_data:
